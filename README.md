# DNAGPT

```bash
python3 projects/dnagpt/finetune/finetune.py --cfg projects/dnagpt/experiments/finetune_{task}.yaml --num-gpus $HOST_GPU_NUM --machine-rank $INDEX --num-machines $HOST_NUM --dist-url $DIST_URL
```
